export enum ButtonIntent {
  Neutral = 'neutral',
  Accent = 'accent',
  Warning = 'warning',
  Error = 'error',
}

export enum ButtonVariant {
  Filled = 'filled',
  Minimal = 'minimal',
}

export enum ButtonSize {
  Default = 'default',
  Small = 'small',
}
